# The model library

!!! note "Under construction"
    This page is a placeholder — content coming soon.

<!-- Scope: the built-in library of network architectures shipped with Ais (~15 of them),
     as listed by `ais train -models` and picked in the GUI training panel / with `-a`.
     Covers: the VGGNet family (M, L), UNet (L), the ezm-2d-* family (same backbone,
     different loss functions), and pix2pix (the one adversarial / conditional-GAN net);
     2D vs 2.5D vs true-3D variants (the `3d`-named nets and their minimum slab depth);
     how to choose one (link to guide/training.md "Choosing an architecture"). Note this
     is distinct from (a) the aiscryoet.org *model repository* of trained, shared models,
     and (b) [Custom model architectures](custom_architectures.md), which is about adding
     your own. Most entries are plain CNNs; pix2pix is the only architecturally different
     one so far. -->
