# Custom model architectures

!!! note "Under construction"
    This page is a placeholder — content coming soon.

<!-- Scope: adding your own network architecture to Ais. Drop a .py file into the
     Ais/models directory that defines: a `title`, an `include` boolean (whether it
     shows up in the software), and a `create(input_shape)` function returning a
     tensorflow.keras model. For non-Keras models, also define a wrapper class that
     implements count_params / fit / predict / save / load. Point to vggnet.py,
     model_template.py, and pix2pix.py as worked examples. (Content exists in the old
     python_module.rst if we want to lift it.) -->
